const expect = require("chai").expect,
	pretty = require('../index.js'),
	config = require('not-config'),
	path = require('path');


describe("pretty middleware", function() {

	it("write some test", function() {
		expect(false).to.deep.equal({});
	});

});
